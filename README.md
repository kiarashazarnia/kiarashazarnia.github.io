# Personal Blog

## Guide

```sh
# generate the new blog in the docs folder had been set in repo settings
hugo build --theme hugo-book --destination docs

hugo version
hugo v0.147.8+extended+withdeploy darwin/arm64 BuildDate=2025-06-07T12:59:52Z VendorInfo=brew

# to run locally
hugo server --theme hugo-book --destination docs
```